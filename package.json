{
  "name": "scmjs-plugin-magenta",
  "version": "0.1.0",
  "private": true,
  "description": "Magenta — a sentence-based trigger editor with native-feeling EUD, a plugin for the scmJS StarCraft map editor.",
  "author": "Jeany",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "esbuild plugin.ts --bundle --format=esm --target=es2022 --platform=browser --loader:.json=json --outfile=dist/plugin.js --banner:js=\"// Generated by npm run build - do not edit; the source is plugin.ts.\"",
    "dev": "npm run build -- --watch",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "maps": "tsx scripts/make-test-maps.mts"
  },
  "devDependencies": {
    "@scm-js/plugin-api": "^1",
    "@types/node": "^24.13.3",
    "esbuild": "^0.28.2",
    "mopaq": "^1.4.0",
    "tsx": "^4.23.0",
    "typescript": "~5.9.2",
    "vitest": "^3.2.4"
  }
}
